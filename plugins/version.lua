do

function run(msg, matches)
  return 'Telegamer '.. VERSION .. [[ 
  default source by seed
  Admins:
  @iranspm_admin
  @mt246
end

return {
    patterns = {
    "^[!/]([Vv]ersion)$"
  }, 
  run = run 
}

end
